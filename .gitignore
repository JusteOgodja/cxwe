logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env
.env.*
!.env.example

# Sauvegardes & données brutes (volumineuses)
supabase/backup_products_*.json
supabase/PRODUITS_CIBLES.csv
supabase/PRODUITS_CIBLES.json
supabase/seed_catalog.sql

# Réglages locaux machine-spécifiques
.claude/settings.local.json

# Export de la base (volumineux)
supabase/export/
supabase/export_*.zip

# Rapports d'analyse (données produit)
supabase/analysis/

# Fichiers d'enrichissement (données produit)
supabase/enrichment/

# Sauvegardes de suppression
supabase/deleted_backup_*.json

# Audit de performance — rapports Lighthouse bruts (volumineux, archivés hors dépôt).
# On ne versionne que les .md de synthèse et les scripts de repro.
performance-audit/lighthouse/before/
performance-audit/lighthouse/after-local/
performance-audit/lighthouse/after-preview/
performance-audit/screenshots/
performance-audit/lighthouse/_*.json
performance-audit*.zip
